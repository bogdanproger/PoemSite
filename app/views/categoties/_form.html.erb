<%= form_with(model: categoty) do |form| %>
  <% if categoty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoty.errors.count, "error") %> prohibited this categoty from being saved:</h2>

      <ul>
        <% categoty.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
